__pycache__/
*.pyc
*.jsonl

# Demo audio. Large binaries, and not ours to redistribute: the folder is the
# deploy source only. On the Pi it lives at ~/pi-glo/music, outside web/, so a
# redeploy cannot delete it.
music/
