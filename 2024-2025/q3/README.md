# Year 1, Quarter 3

Two assignments were referenced here as git submodules:

- `assignment-6-code-analysis-SnakeXrnt`
- `assignment-7-debugging-SnakeXrnt`

Neither was ever committed. They had no `.gitmodules` entry and no URL, so the
recorded pointers could not be fetched and the directories were empty. They were
removed on 2026-08-30. If you still have the original repositories, re-add them
as proper submodules.
